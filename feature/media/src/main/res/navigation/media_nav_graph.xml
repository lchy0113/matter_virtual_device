<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/media_nav_graph"
    app:startDestination="@id/videoPlayerFragment">
    <fragment
        android:id="@+id/videoPlayerFragment"
        android:name="com.matter.virtual.device.app.feature.media.VideoPlayerFragment"
        android:label="VideoPlayerFragment"
        tools:layout="@layout/fragment_video_player">
        <argument
            android:name="setting"
            app:argType="string" />
        <deepLink app:uri="android-app://com.matter.virtual.device.app.feature.media/videPlayerFragment/{setting}" />
    </fragment>
</navigation>